version: '3'
services:
  database:
    image: "postgres:12"
    environment:
      - DB_USER=username
      - DB_PASSWORD=password
      - DB_NAME=doodle_chat
    volumes:
      - database-data:/var/lib/postgresql/data/
    ports:
      - 5432:5432
volumes:
  database-data: